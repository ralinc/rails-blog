= simple_form_for @article do |f|
  = f.input :title, placeholder: 'Title', label: false
  = f.input :slug, placeholder: 'Slug', label: false
  = f.input :content, as: :text, placeholder: 'Content', label: false
  = f.button :submit, 'Save'
